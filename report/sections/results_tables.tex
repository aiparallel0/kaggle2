\section{Extended Results Tables}
\label{sec:results_tables}

This section collects the detailed quantitative tables that Section~V
cites.  Every number is sourced from
\texttt{runs/<run\_id>/metrics/} sidecars via the
\texttt{report.inject} substitution layer --- no literal constants
appear in this file.  An empty cell (``---'') indicates the
corresponding sidecar was not produced on this run; the cell's key is
enumerated in \texttt{metrics/unresolved\_vars.json} for audit.

\begin{table}[t]
\centering
\caption{Headline per-field F1 ($\%$). DONUT vs.\ the YOLO$+$TrOCR$+$Attention
         pipeline vs.\ the rule-based GT-OCR baseline.}
\label{tab:headline_f1}
\small
\VAR{table_headline_f1}
\end{table}

\begin{table}[t]
\centering
\caption{Extended per-field metrics (P, R, F1, exact-match) for DONUT
         and the learned-assigner pipeline.  Produced by
         \texttt{core.metrics\_extended} over the same 63-image test
         split as Section~V headline.}
\label{tab:extended}
\scriptsize
\VAR{table_extended}
\end{table}

\begin{table}[t]
\centering
\caption{Latency and cost per image, per system.  Mean and percentiles
         are in milliseconds; throughput is images/second at batch
         size~1.  USD/image is the amortised cost on the reported
         hardware.  Cells render as a typed \texttt{\textbackslash
         MissingCell\{$\cdot$\}} marker when the latency producer
         (\texttt{core.metrics\_latency}) did not run on this build
         --- never as a silent em-dash.}
\label{tab:latency}
\small
\resizebox{\columnwidth}{!}{%
\VAR{table_latency}%
}
\end{table}

\begin{table}[t]
\centering
\caption{Training-run summary: number of epochs, best-checkpoint
         epoch, wall-clock seconds, peak VRAM, total USD, and total
         energy.  Produced by \texttt{core.energy} combined with the
         per-epoch telemetry logs.  The table is auto-fitted to
         \texttt{\textbackslash columnwidth} via \texttt{\textbackslash
         resizebox} so the rightmost CO\textsubscript{2}eq column
         never gets truncated to ``CO'' (a recurring v3 regression).}
\label{tab:training}
\small
\resizebox{\columnwidth}{!}{%
\VAR{table_training}%
}
\end{table}

\begin{table}[t]
\centering
\caption{Zero-shot foundation-model ceiling.  A frontier multimodal
         LLM is prompted once per test image to emit the SROIE field
         JSON; no fine-tuning.  The \emph{Source} column is the
         single-source-of-truth signal: \texttt{measured} cells come
         from a live API call cached in
         \texttt{metrics/foundation\_metrics.json}; \texttt{fixture}
         cells come from \texttt{results/foundation\_baseline.json},
         a conservative zero-shot prior drawn from contemporary
         frontier-model evaluations on receipt KIE.  Fixture rows
         are explicitly marked --- the v3 ambiguity (where a fixture
         could be misread as measured) is resolved by this column.
         Setting \texttt{config.foundation\_enabled=true} runs the
         live API once per test image, caches by SHA-256 of image
         bytes, and overwrites the fixture row with a measured one.}
\label{tab:foundation_ceiling}
\small
\begin{tabular}{lcccl}\toprule
System & F1 & NED & EM & Source \\ \midrule
Frontier LLM (zero-shot)           & \VAR{foundation_f1} & \VAR{foundation_ned} & \VAR{foundation_em} & \texttt{\VAR{foundation_source}} \\
DONUT (ours, fine-tuned)           & \VAR{donut_f1}      & \VAR{donut_ned}      & \VAR{donut_em}      & \texttt{measured} \\
Pipeline (ours, fine-tuned)        & \VAR{pipeline_f1}   & \VAR{pipeline_ned}   & \VAR{pipeline_em}   & \texttt{measured} \\
\midrule
\multicolumn{5}{l}{\emph{Frontier LLM per-field token-F1}} \\
\quad company & \VAR{foundation_f1_company} & \multicolumn{2}{c}{\textit{n/a}} & \texttt{\VAR{foundation_source}} \\
\quad date    & \VAR{foundation_f1_date}    & \multicolumn{2}{c}{\textit{n/a}} & \texttt{\VAR{foundation_source}} \\
\quad address & \VAR{foundation_f1_address} & \multicolumn{2}{c}{\textit{n/a}} & \texttt{\VAR{foundation_source}} \\
\quad total   & \VAR{foundation_f1_total}   & \multicolumn{2}{c}{\textit{n/a}} & \texttt{\VAR{foundation_source}} \\
\bottomrule
\end{tabular}
\end{table}
